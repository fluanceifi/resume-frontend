# ── 서버(서버리스 함수)용 ──
# Vercel 프로젝트 Settings → Environment Variables 에 등록. 클라이언트로 노출되지 않는다.
OPENAI_API_KEY=sk-...

# ── 클라이언트(빌드타임)용, 선택 ──
# 프론트와 API를 같은 Vercel 프로젝트로 배포하면 불필요(기본 /api/chat 사용).
# GitHub Pages 등 분리 배포 시에만 API 절대 URL 지정.
# VITE_CHAT_ENDPOINT=https://your-app.vercel.app/api/chat
